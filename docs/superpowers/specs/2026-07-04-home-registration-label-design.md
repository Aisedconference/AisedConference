# Home Registration Label Design

## Goal

Change the homepage primary action label from `Registration Portal` to `Registration`.

## Design

- Update only the visible text in the homepage hero action.
- Keep the link target `registration.html`, button styling, position, and behavior unchanged.
- Add a focused regression check and run the full site test suite.
